import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function InboxScreen() {
  return (
    <PlaceholderScreen
      title="Inbox"
      description="Capture tasks as they come. Sort them later."
    />
  );
}
